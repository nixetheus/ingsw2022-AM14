package it.polimi.ingsw.model.controller;

import it.polimi.ingsw.controller.LoginController;
import org.junit.Assert;
import org.junit.Test;

/**
 * LoginControllerTest tests the LoginController methods
 */
public class LoginControllerTest {

  /**
   * This method tests if the map fot the id and the player nickname will be filled correctly
   */
  @Test
  public void testAddPlayer() {
   // LoginController loginController = new LoginController();
   // loginController.addPlayer("Ale");
    // loginController.addPlayer("Luca");
    // loginController.addPlayer("Dario");

    // Assert.assertEquals(loginController.getPlayerIdNicknameMap().get(0), "Ale");
    // Assert.assertEquals(loginController.getPlayerIdNicknameMap().get(1), "Luca");
    // Assert.assertEquals(loginController.getPlayerIdNicknameMap().get(2), "Dario");
  }

  @Test
  public void testAddPLayerDuplicated() {
    //LoginController loginController = new LoginController();
    // loginController.addPlayer("Ale");
    // loginController.addPlayer("Ale");

    //error message
  }

}
